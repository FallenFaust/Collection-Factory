.env

# Internal notes: work status, the reasoning behind the content rules, the language
# convention. The team needs them, the client does not — so they live next to the
# repository rather than inside it.
internal.md
__pycache__/
*.pyc
.venv/
venv/

# Runs. A single production run of three sets is a few hundred images, and the tool
# regenerates them from the theme in one press — so the output is not what this
# repository is for. The code, the prompts and the journal are.
runs/

# Working output of individual stages, kept locally
sets/
pipeline/lora_test_out/
pipeline/style_probe_out/
pipeline/shadow_probe_out/
_to_delete/
*.safetensors
*.ckpt

.DS_Store
Thumbs.db
